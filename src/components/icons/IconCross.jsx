import React from 'react';

const IconCross = () => (
  <span role="img" aria-label="plus" style={{fontSize: "10px", color: "transparent", textShadow: "0 0 0 black"}}>❌</span>
);

export default IconCross;
