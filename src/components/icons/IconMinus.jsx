import React from 'react';

const IconMinus = () => (
  <span role="img" aria-label="plus" style={{fontSize: "3.0vw", color: "transparent", textShadow: "0 0 0 black"}}>➖</span>
);

export default IconMinus;
